# Spec.md — "테스트할개" (브랜드명 확정, 도메인 확정)

이 문서는 기획/디자인 담당(클로드 챗)이 작성하고, 개발 담당(클로드 코드)이 참고하는 지시서다.
현재 상태 기록은 `PROJECT_STATUS.md`를 계속 참고할 것 — 이 문서는 "다음에 뭘 만들어야 하는가"만 다룬다.

작성일: 2026-07-09
작성자: 클로드 챗 (기획/디자인/검토)

## 브랜드명 확정 (2026-07-09)

- 사이트명: **"테스트할개"** (2026-07-11, "테스트할게"에서 재변경 확정 — 시바견 로고 컨셉과 연결되도록 "게"→"개" 말장난)
- 근거: "~할게" 말투 트렌드 활용, 문장형 네이밍이라 기존 "테스트+명사" 경쟁 사이트(테스트잇/테스트모아/테스트하로 등)들과 차별화됨. 신조어가 아니라 표준 한국어 어미라 유행이 식어도 이름 자체는 안 낡음.
- **도메인 확정**: `willtest.kr` (가비아 구매 완료, 2026-07-10). Vercel 프로젝트에 커스텀 도메인으로 연결하고, `NEXT_PUBLIC_SITE_URL`을 `https://willtest.kr`로 설정할 것. 코드/카피에서 "MBTIer" 또는 "테스트할게" 텍스트를 찾는 대로 "테스트할개"로 교체.

---

## 0. 최우선 순위 원칙

**일정 전제**: 주말에만 작업, 총 1개월 예상. 배포가 긴급하지 않으므로, 아래처럼 "배포 인프라 준비"와 "공개 오픈(+애드센스 신청)"을 분리한다.

- **배포 인프라 준비**(GitHub 브랜치 정리, Vercel 연결, GA 연동)는 1주차에 먼저 끝내둔다. 이후 매주 작업분을 Vercel 미리보기 URL로 실제 확인하며 진행 — 로컬에서만 보는 것보다 안전하고, 마지막에 몰아서 검증할 필요가 없어짐.
- **공개 오픈 + 애드센스 신청**: 2026-07-11 기준 사장님이 배포를 의도적으로 미루기로 함 — 디자인/공유 기능 등 남은 보강 작업을 먼저 다 끝낸 뒤 재논의. Vercel 실제 연결도 아직 안 한 상태(로컬 빌드만으로 검증 계속해옴).

**주차별 제안 일정 (2026-07-10 수정 — MBTI 실검사 추가로 1주 늘림)**

| 주차 | 작업 |
|---|---|
| 1주차 (완료) | 배포 인프라(Phase 1) + `testEngine.js` 공통 아키텍처 설계 + 테토-에겐 12문항 초안 |
| 2주차 | **MBTI 실제 검사 신규 추가**(4축×6문항=24문항, testEngine 재사용, 결과는 기존 `/mbti/[type]` 재활용) + 테토-에겐 12→25문항 확장 |
| 3주차 | 감다살/감다뒤 (가벼움) + HSP 자애/타애 민감형 (문항 카피 신중하게, 사장님 검토 필요) |
| 4주차 | 성인 애착유형 + 홈 화면 재설계(햄버거 메뉴, 카드 그리드) + MBTI 궁합 콘텐츠 살짝 보강 |
| 5주차 | 디자인 마감(컬러 토큰, 모션), 공유 기능(OG), 최종 점검 → 공개 오픈 + 애드센스 신청 |

이 순서는 권장안이며, 실제 진행 속도에 따라 클로드 코드가 유동적으로 조정 가능. 다만 **HSP/애착유형처럼 실제 심리검사가 베이스인 콘텐츠는 물론, MBTI 실검사 문항도 반드시 사장님 검토 거친 뒤 확정**하는 원칙은 유지.

---

## Phase 1 — 배포 전 필수 (개발 작업, 디자인 변경 최소)

이 항목들은 디자인 판단이 필요 없는 순수 개발 작업이라, 별도 스펙 없이 클로드 코드에게 바로 맡겨도 된다.

1. GitHub 저장소 정리 — 기본 브랜치를 `master`로 통일 (현재 `main`엔 코드가 없음)
2. Vercel 배포 (GitHub 연결 → 자동 배포)
3. `NEXT_PUBLIC_SITE_URL` 환경변수 설정 (sitemap이 localhost 기준으로 나가고 있음)
4. Google Analytics(GA4) 연동 — 유입 채널 확인용, 애드센스 신청 전에 데이터 쌓기 시작해야 함
5. `public/eb9da986-....png` (원본 그리드 시트, 2.5MB) 등 코드에서 안 쓰는 용량 큰 파일은 배포 이미지에서 제외 검토

---

## Phase 2 — 디자인/콘텐츠 스펙 (아래부터가 이 문서의 본론)

### 1. 디자인 시스템

현재 상태: 시스템 폰트, 포인트 컬러 `#2f6fed` 파란색 하나, 라이트/다크 자동 전환만 있음. 나쁘지 않지만 "재미 테스트"치고는 톤이 너무 차분하다 (금융 앱 같은 느낌).

**컬러 토큰 (제안)**
- Primary: `#2f6fed` (기존 파란색 유지 — 이미 여러 곳에 쓰여서 바꾸면 수정 범위가 큼)
- Accent (CTA/포인트 강조용, 신규): `#FF4B6E` (활기찬 코럴핑크 — 버튼 hover, 궁합 점수 숫자, 공유 버튼에만 제한적으로 사용)
- Background: 기존 유지 (라이트 `#fff`, 다크 `#0a0a0a` 계열로 추정)
- 텍스트 컬러 토큰화: 지금 하드코딩된 `#666`, `#aaa`, `#eaeaea` 등을 `globals.css`에 CSS 변수로 선언해서 `page.module.css` 등 여러 파일에서 재사용 (일관성 + 나중에 톤 조정 쉬움)

```css
:root {
  --color-primary: #2f6fed;
  --color-accent: #ff4b6e;
  --color-text-muted: #666;
  --color-border: #eaeaea;
}
@media (prefers-color-scheme: dark) {
  :root {
    --color-text-muted: #aaa;
    --color-border: #2a2a2a;
  }
}
```

**타이포그래피**
- 지금 시스템 폰트로 충분함 (로딩 속도 우선). 폰트 교체는 Phase 3(트래픽 붙은 후)로 미룬다.
- 궁합 점수(`score`)만 크게 강조 — 지금 `.score` 클래스가 얼마나 큰지 코드 기준 불명확했는데, 결과 페이지의 "얼굴"이 되어야 하는 숫자이니 32px 이상 + Accent 컬러 + bold로.

**우선순위**: 컬러 변수화는 낮은 리스크/공수로 전체 톤을 살릴 수 있어서 Phase 1 직후 바로 처리해도 좋음. (CSS 변수로 토큰화하므로 이후 팔레트 조정도 값만 바꾸면 됨 — 리스크 낮음)

**모션/애니메이션 가이드 (신규 — 이미지 추가 대신 UX 힘싣기)**

이미지 에셋을 늘리는 대신, 가벼운 CSS 트랜지션으로 완성도를 높인다. 성능(Core Web Vitals)과 SEO에 영향 없도록 무거운 JS 애니메이션 라이브러리는 쓰지 않고 순수 CSS로 처리한다.

- 결과 점수 카운트업: `/mbti/match/[pair]` 진입 시 궁합 점수가 0에서 실제 점수까지 숫자가 빠르게 올라가는 연출 (결과 "공개 순간"의 임팩트를 살림 — 공유하고 싶은 마음도 커짐)
- 테스트 카드 등장 애니메이션: 홈 화면 테스트 카드 그리드가 순차적으로 살짝 올라오며 fade-in (stagger, 카드당 30~50ms 지연)
- 기존 hover 스케일 효과(`.typeCard:hover`) 유지, 결과 섹션 카드에도 동일한 톤의 hover/tap 효과 확장
- 성별 토글, 카테고리 탭 전환 시 배경색 슬라이드 트랜지션 (지금도 `transition: background 0.15s`는 있음 — 유지하고 다른 토글류에도 일관 적용)
- `prefers-reduced-motion: reduce` 미디어 쿼리 대응 필수 — 모션에 민감한 사용자를 위해 애니메이션 비활성화 옵션 존중

---

### 2. 바이럴 공유 기능 (신규 — 가장 중요한 추가 기능)

한국 MBTI 테스트 사이트가 트래픽을 얻는 핵심 경로는 **카카오톡 공유 시 미리보기(링크 카드)**다. 지금은 이 부분이 전혀 없다.

**요구사항**
1. `/mbti/match/[pair]` 페이지에 Open Graph 메타 태그 추가
   - `og:title`: 예) "ESTJ × INFP 궁합, 결과는?"
   - `og:description`: 궁합 점수 + 한 줄 요약 (섹션 콘텐츠에서 발췌, 각 궁합마다 자동 생성)
   - `og:image`: 남녀 이미지 페어를 합성한 정적 카드 이미지 (아래 3번 참고)
2. "카카오톡으로 공유하기" 버튼을 결과 페이지 상단에 추가 (Web Share API 우선 사용, 미지원 브라우저는 URL 복사로 폴백 — 카카오 SDK까지는 Phase 1에서 필요 없음, 네이티브 공유로 충분)
3. `og:image`용 결과 카드 이미지는 정적 생성이 어렵다면(136개 조합 x 2 성별 = 272장 사전 렌더링은 과함), Next.js의 `ImageResponse`(OG Image Generation)로 요청 시 동적 생성하는 방식을 클로드 코드에게 검토 요청

**우선순위**: Phase 1 직후 최우선 — 검색 유입 이전에 "공유로 인한 자연 유입"이 더 빠르게 트래픽을 만들어 줄 수 있음.

---

### 3. 홈 화면 콘텐츠 보강 (SEO/애드센스용)

현재 홈 화면은 제목+한 줄 부제만 있어서 텍스트 양이 매우 적다. 애드센스는 첫 페이지도 심사 대상이다.

**추가 제안**
- 폼 아래, 유형 카드 그리드 위에 3~4문단짜리 소개 섹션 추가: "MBTI 궁합이란", "이 테스트는 어떻게 계산되나요" 같은 설명형 텍스트
- 어렵게 새로 쓸 필요 없음 — 이미 `compatibilityContent.js`에 있는 4개 차원(E/I, S/N, T/F, J/P) 설명을 재활용해서 "궁합은 이 4가지 기준으로 비교합니다"처럼 풀어쓰면 됨

**주의**: 텍스트를 늘리는 목적이 "심사 통과"이지 "장문의 벽" 만들기가 아님 — 3~4문단 선에서 멈출 것.

---

### 4. 결과 페이지 유료화 UI 처리

`ResultView.js`의 "500원 결제하고 확인하기" 버튼이 `PAYWALL_ENABLED = false`인데도 코드에 남아 있다. 지금은 안 보이니 기능상 문제는 없지만, 만약 사장님이 "무료 콘텐츠 확장"에 집중하는 동안 실수로 플래그가 켜지면 개발 안 된 결제 버튼이 그대로 노출된다.

**제안**: `handleUnlock` alert 문구를 그대로 두되, Phase 1에서는 이 플래그가 확실히 false로 배포되는지만 확인. 결제 연동은 Phase 3(트래픽 검증 후)로 명확히 미룬다 — 지금 손대지 않음.

---

### 5. 광고 영역 — 애드센스 승인 전까지는 placeholder 유지 (2026-07-10 추가)

`AdSlot.js`가 지금 "광고 영역 준비중" 점선 박스로 표시되고 있는데, **애드센스 승인 나기 전까지는 이 상태 그대로 두고, 승인 후에만 실제 광고 코드로 전환**한다.

- `GoogleAnalytics.js`에서 썼던 패턴(환경변수 값이 있을 때만 스크립트 로드)을 그대로 재사용: `NEXT_PUBLIC_ADSENSE_CLIENT_ID`가 설정되어 있으면 실제 애드센스 스크립트(`adsbygoogle.js`)와 `<ins>` 광고 단위를 렌더링하고, 없으면 지금처럼 placeholder 박스를 보여준다.
- 즉 코드는 지금 미리 준비해두되(`AdSlot.js`에 조건 분기 추가), 실제 전환은 애드센스 승인 받고 `NEXT_PUBLIC_ADSENSE_CLIENT_ID`(및 슬롯 ID)를 Vercel 환경변수에 넣는 순간 자동으로 이루어지게 한다 — 코드 재배포 불필요.
- 애드센스 심사 신청 자체는 광고가 아직 안 보여도 상관없음(placeholder 상태로도 신청 가능) — 콘텐츠 품질을 보는 것이지 광고 노출 여부를 보는 게 아님.

---

## Phase 2.5 — 멀티 테스트 확장 ("테스트 모음 사이트"로 전환)

사이트 컨셉을 MBTI 단일 사이트에서 **테스트 모음 사이트**로 확장한다. 이유: 콘텐츠 종류가 다양해야 애드센스 심사에 유리하고, 테스트 간 내부 링크로 세션 체류시간이 늘어남. 리서치 결과, MBTI·테토-에겐·HSP(예민형)·애착유형 테스트는 실제로 MZ세대 사이에서 "정체성 테스트"라는 하나의 트렌드 군집으로 함께 소비되고 있어 궁합이 좋다.

**주의**: 이 단계는 MBTI 단독 배포(Phase 1) *이후*에 진행한다. 배포를 늦추는 이유로 쓰지 말 것.

### 2.5.1 아키텍처 변경

- 새 테스트는 `src/app/tests/{slug}/...` 공통 라우트로 추가. 기존 `/mbti/...`는 이미 SEO 인덱싱 대상이 될 URL이라 **손대지 않고 그대로 유지**.
- 데이터 폴더링: `src/data/tests/{slug}/questions.js`, `src/data/tests/{slug}/results.js`로 테스트별 분리.
- 채점 로직 공통화: 문항 응답 → 점수 계산 → 결과 매핑까지의 공통 유틸을 `src/lib/testEngine.js`로 뽑아서 4개 신규 테스트가 재사용.
- 공유(OG 이미지), AdSlot 등 기존 컴포넌트는 테스트 무관하게 재사용 가능하도록 props만 일반화.

### 2.5.2 홈 화면 재설계 (허브형, poomang 레퍼런스 참고)

사장님이 공유한 레퍼런스(poomang) 중 지금 단계에 맞는 요소만 채택한다.

**채택**
- 상단 햄버거 메뉴(☰): 클릭 시 카테고리 목록(`전체 / 성격유형 / 감정심리`)이 드로어로 열림. 데스크톱/모바일 공통으로 이 구조 사용 — 지금의 상단 탭 방식보다 테스트 개수가 늘어나도 확장성 좋음.
- "NEW" 라벨: 최근 추가된 테스트 카드에 뱃지 표시 (신규 테스트 눈에 띄게)
- 테스트 카드 그리드: 정사각형 썸네일 + 제목 + 한 줄 설명 + 예상 소요시간("3분") + 카테고리 pill 태그
- 기존 "나/상대방 MBTI 선택 폼"은 홈 최상단에서 빼고, 카드 그리드 안의 "MBTI 궁합" 카드를 클릭하면 그 폼이 있는 화면으로 이동 (홈이 특정 테스트에 종속되지 않도록)
- 신규 소개 문단(Phase 2의 3번)은 "이 사이트는 무엇인가"로 톤 조정 — MBTI 전용 설명에서 테스트 모음 전체 소개로 업데이트

**채택하지 않음 (지금 단계엔 과함, Phase 3 이후 재검토)**
- 상단 회전형 대형 히어로 배너 — 테스트가 4~5개뿐인 지금은 배너에 넣을 콘텐츠가 부족함. 테스트가 8개 이상 쌓이면 재검토.

**절대 하지 말 것 — 참여 수 뱃지 조작 금지**
- 레퍼런스엔 카드마다 "4801", "5516" 같은 참여자 수가 붙어 있는데, **실제 GA/참여 데이터 없이 숫자를 지어내면 안 된다.** 가짜 사회적 증거는 사용자를 속이는 것이고, 나중에 실제 숫자와 괴리가 생기면 신뢰도 문제가 된다. GA 연동 후 실제 참여 수가 쌓이면 그때 진짜 숫자를 노출한다. 그전까지는 숫자 없이 "NEW" 뱃지나 카테고리 pill만 사용.

### 2.5.3 디자인 — 카테고리 컬러 확장

기존 Primary(`#2f6fed`)/Accent(`#ff4b6e`)에 카테고리 구분용 컬러 하나 추가:

```css
:root {
  --color-category-personality: #2f6fed; /* 성격유형 (MBTI, 테토-에겐) */
  --color-category-emotion: #8b7cf6;     /* 감정심리 (HSP, 애착유형, 감다살) */
}
```

테스트 카드의 카테고리 pill과 결과 페이지 헤더 컬러에 사용. 두 컬러 모두 Accent 코럴핑크와 톤이 겹치지 않도록 확인함.

### 2.5.4 신규 테스트 4종 개요 (문항 상세는 다음 라운드에서 확정)

| 순서 | 테스트 | 카테고리 | 개요 |
|---|---|---|---|
| 1 | 테토-에겐 | 성격유형 | 호르몬 컨셉 2형 분류, 약 12문항 |
| 2 | HSP 자애/타애 민감형 | 감정심리 | 예민도 + 민감 방향(자기/타인) 2축, 학술 HSP-R 문항의 재미용 축약판 |
| 3 | 성인 애착유형 | 감정심리 | 안정/불안/회피/혼란 4유형, ECR 척도 기반 축약판 |
| 4 | 감다살/감다뒤 데일리 체크 | 감정심리 | 5문항 초경량, "센스/눈치가 오늘 살아있나 죽어있나"를 재는 밈 활용(감정 상태 아님 — 2026-07-10 정정), 매일 다른 결과 가능(재방문 유도용) |

**중요 — 신뢰성 관련 주의사항**: HSP와 애착유형은 실제 학술 심리검사(HSP-R, ECR)를 모티프로 한다. 재미 콘텐츠로 가볍게 각색하되, 각 테스트 하단에 "이 테스트는 재미로 즐기는 자가진단이며 전문 심리검사를 대체하지 않습니다" 안내 문구를 반드시 넣는다 — 실제 임상적 진단처럼 오해되지 않도록.

### 2.5.5 콘텐츠 품질 보강 (2026-07-10 추가 — 사장님 지적사항 반영)

**① MBTI 실제 검사 신규 추가 (가장 중요)**
기존 홈 화면은 "이미 아는 MBTI를 선택"하는 방식뿐이었고, 실제 문항으로 유형을 알아내는 검사 자체가 없었다 — "테스트 사이트"의 핵심 기능이 빠져 있던 것. 아래처럼 보완한다.

- 신규 검사: EI/SN/TF/JP 4축 × 6문항 = 24문항. `testEngine.js`의 다축(multi-axis) 구조를 그대로 재사용 (2축→4가지 결과였던 것을 4축→16가지 결과로 확장하는 것뿐, 엔진 수정 불필요).
- 결과는 새 페이지를 또 만들지 않고 기존 `/mbti/[type]`, 상대방까지 입력했다면 `/mbti/match/[pair]`로 바로 연결 — 기존 콘텐츠 재사용.
- 홈 화면엔 "이미 유형을 아신다면 [직접 선택]" / "모른다면 [검사로 알아보기]" 두 경로를 나란히 제공.
- 문항 카피는 초안 작성 후 반드시 사장님 검토.

**② MBTI 궁합 콘텐츠 살짝 보강**
기존 4개 차원(E/I, S/N, T/F, J/P) 조합 생성 로직(`compatibilityContent.js`)은 그대로 두고, 각 섹션 설명 분량만 한 문단 → 한 문단 반 정도로 소폭 확장. 전면 재작성 아님 — 지금 있는 글의 결을 유지하면서 살만 붙이는 수준.

**③ 테토-에겐 12→25문항 확장**
아래 13문항(q13~q25)을 기존 12문항에 이어 추가한다. 형식은 기존과 동일(2지선다, `scores: { teto: 1 }` / `{ teto: -1 }`).

| # | 질문 | A (teto+1) | B (teto-1) |
|---|---|---|---|
| q13 | 여행 계획 짤 때 나는? | 일정표부터 딱 짜고 움직인다 | 가서 그때그때 정하는 걸 좋아한다 |
| q14 | 갖고 싶은 물건이 있을 때 나는? | 바로 사러 간다 | 며칠 고민하다 산다 |
| q15 | 게임/승부에서 질 것 같으면 나는? | 끝까지 이기려고 덤빈다 | 적당히 즐기는 데 의의를 둔다 |
| q16 | 칭찬을 받으면 나는? | "그치? 나 잘하지" 하고 받아들인다 | "아니야 아니야" 하며 손사래친다 |
| q17 | 화가 났을 때 나는? | 표정/말투에 바로 티가 난다 | 티 안 내려고 애쓴다 |
| q18 | 새로운 도전 앞에서 나는? | 일단 저지르고 본다 | 준비가 될 때까지 미룬다 |
| q19 | 약속 시간에 나는? | 딱 맞춰서 가거나 살짝 늦는 편 | 늦을까봐 미리 여유있게 도착한다 |
| q20 | 좋아하는 사람이 생기면 나는? | 티 내고 먼저 다가간다 | 티 안 내고 혼자 마음 정리부터 한다 |
| q21 | 돈 관련 결정을 내릴 때 나는? | 필요하다 싶으면 바로 쓴다 | 여러 번 재고 나서 쓴다 |
| q22 | SNS에서 논쟁을 보면 나는? | 댓글로 내 생각을 남기는 편 | 조용히 읽고 넘어간다 |
| q23 | 새 취미를 고를 때 나는? | 몸으로 부딪히는 활동적인 걸 고른다 | 혼자 조용히 할 수 있는 걸 고른다 |
| q24 | 위로를 받고 싶을 때 나는? | "나 힘들어" 하고 직접 말한다 | 알아서 눈치채주길 바란다 |
| q25 | 계획이 틀어졌을 때 나는? | 바로 플랜B로 넘어간다 | 잠시 멈춰서 다시 생각을 정리한다 |

### 2.5.6 감다살/감다뒤 콘텐츠 정정 (2026-07-11 — 의미 오해 수정)

**정확한 의미**: "감다살"("감 다 살았네")은 감정 상태가 아니라 **센스·눈치·타이밍(감각)이 오늘 살아있는지**를 뜻하는 밈이다. 스트리머/친구가 재치 있게 상황을 캐치했을 때 쓰는 칭찬 표현. 반대말 "감다뒤"("감 다 뒤졌다")는 센스가 완전히 죽어서 눈치 없거나 타이밍을 놓쳤을 때 쓰는 표현이다. "감정이 예민한 날/무던한 날"로 구현된 기존 초안은 오해에서 나온 것이라 아래로 교체한다.

문항 5개(기존 `GAMDASAL_QUESTIONS`와 동일한 스키마, `scores: { gamdasal: 1 }` / `{ gamdasal: -1 }`)와 결과 콘텐츠를 아래로 교체한다. `dailyBias.js`는 콘텐츠와 무관하게 동작하므로 그대로 재사용.

| # | 질문 | A (gamdasal+1, 센스 살아있음) | B (gamdasal-1, 센스 죽음) |
|---|---|---|---|
| q1 | 대화 중 갑자기 정적이 흐르면 나는? | 센스있게 화제를 바로 돌린다 | 나도 같이 어색해져서 아무 말도 못한다 |
| q2 | 친구 텐션이 평소랑 다르면 나는? | 바로 눈치채고 먼저 말을 건다 | 한참 지나서야 "무슨 일 있었어?"라고 뒤늦게 묻는다 |
| q3 | 단체 카톡방에서 나는? | 타이밍 좋게 드립 치고 리액션도 잘 받는 편 | 타이밍 놓쳐서 이미 지나간 얘기에 뒤늦게 답장한다 |
| q4 | 상대방 표정만 보고도 나는? | 기분 상태를 바로 캐치한다 | 나중에 듣고서야 "아 그랬구나" 한다 |
| q5 | 오늘 하루 전체적으로 나는? | 뭘 해도 척척 재치있게 풀리는 느낌 | 뭘 해도 한 박자씩 늦거나 어긋나는 느낌 |

**결과 콘텐츠**
- `gamdasal` (title: "오늘의 감다살", tagline: "센스가 제대로 살아있는 날") — description: "오늘은 눈치도 타이밍도 다 딱딱 맞아떨어지는 날이에요. 대화든 상황 판단이든 한 박자 빠르게 캐치해서, 옆에서 보면 '오늘 감 다 살았네' 소리가 절로 나올 것 같은 컨디션입니다." tip: "이런 날은 중요한 대화나 협상, 눈치 싸움이 필요한 자리에 특히 유리해요."
- `gamdawi` (title: "오늘의 감다뒤", tagline: "센스가 자리를 비운 날") — description: "오늘은 타이밍이 자꾸 한 박자씩 어긋나는 날이에요. 눈치껏 캐치해야 할 순간을 놓치거나, 반응이 좀 늦게 나올 수 있습니다. 너무 자책하지 말고 오늘은 그냥 무던하게 넘어가는 날로 받아들이세요." tip: "중요한 결정이나 눈치가 필요한 대화는 컨디션 좋은 날로 미뤄두는 게 나을 수 있어요."

### 2.5.7 공통 레이아웃 — 헤더/푸터 (2026-07-11 추가)

지금은 페이지마다 내비게이션이 제각각이라(홈 화면에만 햄버거 메뉴, 일부 페이지엔 `BackButton`, 나머지는 아예 없음), 테스트 페이지에 들어간 사용자가 다른 테스트나 홈으로 이동할 길이 없는 경우가 있다. `src/app/layout.js`에 공통 헤더/푸터를 추가해 모든 페이지에 일관되게 적용한다.

**헤더 (`SiteHeader`)**
- 좌측: 햄버거 메뉴(☰) — 홈 화면에 페이지 전용으로 만들어져 있던 카테고리 드로어(전체/성격유형/감정심리)를 여기로 옮겨서 전 페이지 공통으로 재사용
- 중앙: "테스트할개" 워드마크 텍스트, 클릭 시 홈으로 이동. **로고 파일 받으면 이 자리에 이미지로 교체** — 지금은 텍스트로 두되, 나중에 이미지 삽입이 쉽도록 별도 컴포넌트(`SiteHeader.js`)로 분리해서 자리 잡아둘 것
- 우측: 지금은 비워둠 (검색/계정 아이콘 등은 Phase 3 이후 필요 시 검토)

**푸터 (`SiteFooter`)**
- 최소한으로: 사이트명 + 저작권 표기, "전체 테스트 보기" 링크 정도만. 과하게 채우지 말 것

**기존 내비게이션과의 정리**
- 홈 화면 자체의 햄버거 버튼은 제거하고 공통 헤더의 것으로 통일
- 궁합↔유형 페이지 간 `BackButton`(router.back())은 그대로 유지 — 공통 헤더의 "홈으로"와 역할이 다름(하나는 뒤로, 하나는 홈으로)이라 중복 아님

---

### 2.5.8 6주차 보강 (2026-07-11 — 배포 계속 미루고 품질/개수 보강 진행)

**① 데스크톱에서도 모바일 레이아웃 강제**

지금 넓은 화면에서 콘텐츠 좌우 여백이 휑하게 비어 보인다는 지적. 두 가지를 같이 적용한다.

- 본문 콘텐츠는 계속 `max-width: 420~480px` 단일 컬럼 유지 (기존 방침 그대로, 이건 이미 되어있음)
- **여백을 그냥 비워두지 않고 채우는 방법 (권장)**: 데스크톱 폭(대략 960px 이상)에서 콘텐츠 좌우에 "다른 테스트 추천" 사이드 레일을 배치한다. `testCatalog.js`에 이미 있는 데이터를 재사용해서 카드 2~3개를 세로로 보여주는 정도로 가볍게. 이건 단순 장식이 아니라 실제 내부 링크가 늘어나는 효과가 있어서 SEO/체류시간에도 도움이 됨.
- 사이드 레일을 넣기 애매한 페이지(문항 진행 중 화면 등)는 배경만 은은한 그라데이션이나 브랜드 컬러 blur 패턴으로 채워서 완전히 빈 여백처럼 안 보이게 처리
- 모바일(768px 미만)에서는 사이드 레일 숨기고 지금처럼 단일 컬럼만

**② HSP / 애착유형 문항 20개로 확장 + 이름 변경**

- "성인 애착유형" → **"애착유형"**으로 명칭 변경 (`testCatalog.js`, `page.js` metadata, 결과 카피 등 전체 그렙해서 교체)
- HSP: 기존 8문항(축당 4개)에 아래 12문항(축당 6개) 추가해서 축당 10문항, 총 20문항으로 확장

| # | 축 | 질문 | poleA(+1) | poleB(-1) |
|---|---|---|---|---|
| sens5 | sensitivity | 강한 빛이나 밝은 조명 아래 있으면 나는? | 눈이 쉽게 피로해지고 신경 쓰인다 | 별로 신경 안 쓰인다 |
| sens6 | sensitivity | 카페인이나 술을 마시면 나는? | 남들보다 반응이 크게 오는 편이다 | 마셔도 별 차이를 못 느낀다 |
| sens7 | sensitivity | 옷 태그나 재질이 조금이라도 거슬리면 나는? | 하루 종일 신경 쓰여서 결국 벗어버린다 | 금방 잊고 신경 안 쓴다 |
| sens8 | sensitivity | 여러 사람이 동시에 말하는 상황에서 나는? | 정신이 없고 쉽게 피곤해진다 | 그런대로 다 따라간다 |
| sens9 | sensitivity | 마감이 임박하면 나는? | 압박감을 크게 느끼고 예민해진다 | 닥치면 하지 하며 크게 동요 안 한다 |
| sens10 | sensitivity | 낯선 곳으로 여행 다녀오면 나는? | 며칠은 그 여운·피로가 오래간다 | 금방 일상으로 돌아온다 |
| dir5 | direction | 누군가 나를 비판하면 나는? | 그 말이 맞는지 내 안에서 오래 곱씹는다 | 상대방이 왜 저런 말을 했을지부터 헤아린다 |
| dir6 | direction | 회의나 모임이 끝나면 나는? | 내가 잘했는지 스스로를 돌아본다 | 다른 사람들 반응이 어땠는지부터 떠올린다 |
| dir7 | direction | 몸이 안 좋을 때 나는? | 내 컨디션에 집중해서 쉰다 | 그래도 주변에 폐 끼치는 게 아닌가 신경 쓴다 |
| dir8 | direction | 그룹 사진을 보면 나는? | 내가 어떻게 나왔는지부터 본다 | 다른 사람들이 어떻게 나왔는지부터 본다 |
| dir9 | direction | 갈등 상황이 생기면 나는? | 내 감정이 다치지 않았는지가 먼저 신경 쓰인다 | 상대방 마음이 다치지 않았을지가 먼저 신경 쓰인다 |
| dir10 | direction | 하루를 마무리할 때 나는? | 오늘 내 기분이 어땠는지 정리한다 | 오늘 주변 사람들 기분이 어땠는지 떠올린다 |

- 애착유형: 기존 10문항(축당 5개)에 아래 10문항(축당 5개) 추가해서 축당 10문항, 총 20문항으로 확장

| # | 축 | 질문 | poleA(+1) | poleB(-1) |
|---|---|---|---|---|
| anx6 | anxiety | 데이트 약속을 잡을 때 나는? | 상대가 진짜 만나고 싶어하는지 자꾸 확인받고 싶다 | 약속대로 만나면 된다고 편하게 생각한다 |
| anx7 | anxiety | 상대방이 SNS에 다른 사람과 찍은 사진을 올리면 나는? | 괜히 신경 쓰이고 이것저것 생각하게 된다 | 그냥 그런가보다 하고 넘긴다 |
| anx8 | anxiety | 연애 초반, 상대의 마음을 확신하기 전까지 나는? | 계속 불안하고 마음을 못 놓는다 | 천천히 지켜보면 된다고 여유롭게 생각한다 |
| anx9 | anxiety | 상대가 "우리 얘기 좀 하자"고 하면 나는? | 순간 안 좋은 일부터 상상하게 된다 | 그냥 무슨 얘기인지 담담하게 기다린다 |
| anx10 | anxiety | 관계에서 나는? | 상대가 날 얼마나 좋아하는지가 늘 마음 한켠에 걸린다 | 굳이 재지 않아도 마음이 편하다 |
| avo6 | avoidance | 연애 초반 상대가 빨리 다가오면 나는? | 부담스러워서 한 발 물러서게 된다 | 오히려 좋고 편하게 받아들인다 |
| avo7 | avoidance | "사랑해" 같은 말을 먼저 하는 것에 대해 나는? | 왠지 낯간지럽고 어색해서 잘 못한다 | 자연스럽게 먼저 표현하는 편이다 |
| avo8 | avoidance | 상대와 미래 계획을 구체적으로 얘기하면 나는? | 왠지 갑갑하고 부담스럽다 | 오히려 설레고 기대된다 |
| avo9 | avoidance | 다투고 난 뒤 화해할 때 나는? | 먼저 다가가기보다 시간을 두고 거리를 둔다 | 빨리 풀고 싶어서 먼저 다가간다 |
| avo10 | avoidance | 연인이 나에 대해 깊이 알고 싶어하면 나는? | 굳이 다 보여줄 필요는 없다고 생각한다 | 알아가는 과정 자체가 좋다 |

결과 콘텐츠(`results.js`)는 두 테스트 다 4가지 결과 유형 구조 그대로 유지 — 문항 수만 늘어나는 것이라 결과 카피 수정 불필요.

**③ 감다살/감다뒤 재확인**: 코드 확인 결과 이미 정정된 "센스/눈치" 버전으로 정확히 반영되어 있음(2026-07-10에 이미 완료). 사장님이 보신 화면은 브라우저 캐시/dev 서버 미재시작으로 추정 — 재확인 요망.

---

### 2.5.9 사이드레일 오적용 수정 + 접근성 보완 (2026-07-11)

**① 사이드레일 수정** — 홈 화면에 사이드레일이 적용되면서 같은 테스트 카드가 좌측 레일/가운데 그리드/우측 레일에 3중 중복되는 문제 발견 (사장님 스크린샷 확인). 원래 의도(다른 페이지에서 다른 테스트로 유도)와 다르게 적용된 기획 실수.

- `SiteShell.js`에서 홈(`/`)도 퀴즈 페이지와 동일하게 사이드레일 없이 배경 패턴만 적용하도록 수정 (`QUIZ_PATH_PREFIX` 체크에 홈 경로 조건 추가)
- 사이드레일은 `/mbti/[type]`, `/mbti/match/[pair]` 같은 개별 콘텐츠/결과 페이지에만 적용 — 지금은 정작 이 페이지들엔 적용이 안 되어 있었으니 이번에 새로 붙일 것
- 추후 신규 테스트(테토-에겐 등) 결과 화면에도 동일하게 적용 검토

**② 접근성 보완**

- `src/app/layout.js`의 `<html lang="en">` → `<html lang="ko">`로 수정
- 홈 화면(`HomeClient.js`)에 `<h1>` 추가 (지금 페이지 10개 중 홈만 유일하게 없음)
- `SiteHeader.js` 카테고리 드로어: 열릴 때 첫 항목(또는 닫기 버튼)으로 포커스 이동, `Esc` 키로 닫기, 닫히면 원래 햄버거 버튼으로 포커스 복귀, `role="dialog"` + `aria-modal="true"` 추가, 열려있는 동안 배경(`body`) 스크롤 잠금
- 각 퀴즈 컴포넌트(`TetoEgenQuiz.js` 등 5개): 문항 전환 시 다음 문항 제목이나 진행률 영역으로 포커스 이동 (키보드/스크린리더 사용자가 진행 상황을 놓치지 않도록)
- 퀴즈 결과 화면에 "홈으로" 링크만 있던 것에 다른 테스트 1~2개 추천 링크 추가 (①의 사이드레일과 별개로, 결과 화면 본문 안에 작게)

---

### 2.5.10 앱 프레임 레이아웃으로 전환 (2026-07-11 — poomang 레�런스 재검토)

사장님이 poomang을 다시 참고해서 확인한 결과, 올바른 방향은 사이드레일이 아니라 **헤더/본문/푸터 전체를 하나의 좁은 프레임으로 묶는 것**이었다. 지금은 헤더만 화면 전체 너비로 늘어나 있고 본문만 좁아서 "PC 화면에 모바일 컴포넌트 하나 얹은 것"처럼 보임.

**적용**
- `layout.js`에서 `SiteHeader` + `SiteShell`(본문) + `SiteFooter`를 통째로 감싸는 `.appFrame` 컨테이너 추가: `max-width: 480px`, `margin: 0 auto`, `min-height: 100vh`, 배경은 `var(--background)`
- `<body>` 자체의 배경은 프레임 바깥을 살짝 다른 톤(라이트 모드는 옅은 회색 `#f2f2f2` 정도, 다크 모드는 지금보다 한 톤 어두운 배경)으로 채워서 프레임 경계가 시각적으로 구분되게 함
- **사이드레일(`SideRail.js`) 사용 중단** — 지금 프레임 바깥 여백은 나중에 애드센스 승인나면 `AdSlot.js`를 재사용해서 실제 광고를 넣을 자리로 남겨둔다 (poomang의 좌우 배너 자리와 동일한 용도). 지금은 그냥 빈 배경으로 둘 것. 컴포넌트 파일은 삭제하지 말고 남겨두되(나중에 다른 용도로 재활용 가능성), import는 제거
- `RelatedTests.js`(결과 화면 내 "다른 테스트도 해보세요" 텍스트 링크)는 그대로 유지 — 이건 사이드레일과 별개로 잘 만들어진 것이고, 모바일에서도 항상 보이는 방식이라 프레임 레이아웃과도 잘 맞음

---

## Phase 3 — 트래픽 확인 후 (지금 스펙에서 제외, 나중에 별도 논의)

- 실제 결제 연동 (토스페이먼츠/포트원)
- ~~폰트 교체, 브랜드 로고/파비콘 제작~~ → 로고(시바견이 테스트하는 모습)는 사장님 지인 디자이너가 외부 제작 예정. 클로드 코드 작업 범위 아님 — 로고 파일 전달받으면 그때 파비콘/헤더 삽입만 진행
- 카카오 공유 SDK로 고급 커스터마이징 (지금은 Web Share API로 충분)
- 다국어 지원 (트래픽 국가 분포 보고 결정)
- 상단 회전형 히어로 배너 (테스트 8개 이상 쌓이면 재검토)
- **신규 테스트 2~3개 추가** (2026-07-10 결정 — 5주 배포 일정 지키기 위해 배포 이후로 미룸)
  - 후보: 이상형 월드컵(검색량 높고 토너먼트 분기라 구현 단순), 밸런스 게임(텍스트만으로 가능, 에셋 부담 없음), 혈액형 궁합(기존 MBTI 궁합 엔진 재활용 가능)
  - 실제 트래픽 확인 후, 어떤 테스트가 반응 좋을지 GA 데이터 보고 우선순위 재조정

---

## 검토자(사장님) 확인 완료 (2026-07-09)

- [x] Accent 컬러 `#FF4B6E` 톤 승인
- [x] 홈 화면 소개 문단 추가 승인
- [x] 공유 기능 Phase 1 직후 최우선 순위 승인

→ 이 스펙은 확정됨. 클로드 코드는 아래 순서로 진행할 것.
